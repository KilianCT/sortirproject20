<form method="post" action="{{ path('app_site_delete', {'id': site.id}) }}" onsubmit="return confirm('Etes vous sur de suprimmer ce site?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ site.id) }}">
    <button class="btn" style="color: red">Supprimer</button>
</form>
