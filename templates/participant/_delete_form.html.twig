<form method="post" action="{{ path('app_participant_delete', {'id': participant.id}) }}" onsubmit="return confirm('Voulez vous suprimer votre compte ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ participant.id) }}" href>
    <button class="btn" style="color: red">Supprimer Son Compte</button>
</form>
