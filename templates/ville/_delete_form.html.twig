<form method="post" action="{{ path('app_ville_delete', {'id': ville.id}) }}" onsubmit="return confirm('Voulez vous vraiment suprimmer cette ville?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ville.id) }}">
    <button class="btn" style="color: red; margin: 0">Supprimer</button>
</form>
